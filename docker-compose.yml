services:
  jokerxcolor:
    build: .
    image: ghcr.io/jokerxhub/jokerxcolor:latest
    container_name: jokerxcolor
    ports:
      - "1314:1314"
    volumes:
      # 数据持久化：把 data 目录挂载出来，容器重建不丢数据
      - ./data:/app/data
    restart: unless-stopped
