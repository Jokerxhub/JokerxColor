/* ============================================================================
 * Jokerx颜色代码 —— 默认分组与颜色数据
 * ----------------------------------------------------------------------------
 * 直接编辑本文件即可管理默认的分组和颜色：
 *   groups          : 分组列表
 *   groups[].name   : 分组名称
 *   groups[].colors : 组内的颜色卡片
 *   colors[].name   : 卡片名称（同一分组内不可重复）
 *   colors[].hex    : 十六进制颜色代码，支持 #RRGGBB 或 #RGB 写法
 *
 * 修改保存后刷新页面即可生效。
 * 注意：如果浏览器里已保存过旧数据，新数据不会自动覆盖，
 * 请在页面底部点「恢复默认数据」后再刷新。
 * ========================================================================== */
window.COLOR_DATA = {
  version: 2,
  groups: [
    {
      name: '默认组',
      colors: [
        { name: '蓝', hex: '#00A9E0' },
        { name: '粉', hex: '#FF003E' }
      ]
    }
  ]
};
